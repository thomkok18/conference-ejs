<%- include('../partials/header'); %>
<main>
    <div class="container">
        <div class="row">
            <div class="col-12">
                <%= cTitle %>
                <%= cDescription %>
                <%= company %>
                <%= size %>
                <%= dateStart %>
                <%= dateEnd %>
                <%= createdAt %>
                <%= updatedAt %>
                <a href="/conference/edit/<%= id %>">Edit</a>
                <form action="/conference/destroy/<%= id %>" method="post">
                    <button type="submit">Delete</button>
                </form>
            </div>
        </div>
    </div>
</main>
<%- include('../partials/footer'); %>